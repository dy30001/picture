# 墨境当前代码逻辑图

日期： 2026-05-02
模式： doc
工作区： `/Users/dy3000/code/pic`

## 1. 一句话结论

当前真实运行逻辑已经是一套主应用：

- 用户访问 `9999`
- Node 服务是 `server/index.mjs`
- 正式成片目录是 `final_4k/`
- `成片/` 只是历史重复目录，不参与现行正式交付口径

## 2. 当前主逻辑图

```mermaid
flowchart LR
    U["用户 / 客户"] --> P["9999 主入口<br/>server/index.mjs"]

    subgraph CLIENT["前端"]
      UI["public/index.html + public/app.js + public/styles.css"]
    end

    subgraph API["主服务接口"]
      T["/api/templates<br/>模板与详情"]
      G["/api/generate<br/>生成"]
      H["/api/history*<br/>历史"]
      A["/api/auth/*<br/>注册登录"]
      C["/api/credits*<br/>积分订单"]
      PAY["/api/payments/*<br/>支付"]
      SP["/studio-previews<br/>成片预览"]
      SR["/studio-review<br/>复核摘要"]
      HEALTH["/api/health"]
    end

    subgraph OUTPUT["业务产物"]
      GEN["generated/<br/>源图与中间结果"]
      REV["review/<br/>接触表 / repair queue / 截图"]
      FINAL["final_4k/<br/>唯一正式成片"]
      LEGACY["成片/<br/>历史重复目录"]
      DATA["data/<br/>历史和账户数据"]
    end

    P --> UI
    UI --> T
    UI --> G
    UI --> H
    UI --> A
    UI --> C
    UI --> PAY
    P --> SP
    P --> SR
    P --> HEALTH

    G --> GEN
    UI --> REV
    SP --> FINAL
    SR --> FINAL
    H --> DATA
    A --> DATA
    C --> DATA
    PAY --> DATA

    LEGACY -.历史兼容.-> FINAL
```

## 3. 摄影棚主流程

```mermaid
flowchart TD
    A["打开 9999"] --> B["摄影棚主界面"]
    B --> C["选场景"]
    C --> D["上传参考照"]
    D --> E["身份状态"]
    E --> F["样片方向预览"]
    F --> G["确认方向后生成"]
    G --> H["批量结果复核摘要"]
    H --> I["正式成片预览"]

    subgraph CLIENT_STATE["前端关键状态"]
      S1["state.studio"]
      S2["studioSamplePreviewSets"]
      S3["样片/成片摘要卡"]
    end

    B --> S1
    F --> S2
    H --> S3
    I --> S3
```

说明：

- 样片阶段和成片阶段都在主应用里
- 当前样片管理还没有彻底抽成单独模块，但已经不再需要第二个独立入口

## 4. 场景生产与交付链路

```mermaid
flowchart LR
    P1["参考图 / 三视图"] --> P2["scripts/newapi_generate_*"]
    P2 --> P3["generated/<br/>源图和中间 4K"]
    P3 --> P4["review/contact_sheets<br/>接触表复核"]
    P4 --> P5["final_4k/<br/>正式成片"]
    P5 --> P6["server/index.mjs<br/>/studio-previews"]
    P5 --> P7["交付摘要 / 下载"]

    L["成片/<br/>历史重复目录"] -.仅历史兼容.-> P5
```

说明：

- 正式交付链路已经收口到 `final_4k/`
- 生成脚本和 执行报告 需要继续跟着这条链路统一

## 5. 当前代码现实

现在最关键的 5 点：

1. 当前主入口是 `9999`
2. 当前主服务是 `server/index.mjs`
3. 当前正式成片目录是 `final_4k/`
4. 样片管理仍需进一步模块化
5. `成片/` 还在磁盘里，但已经不应再被写成并行正式输出

## 6. 这张图怎么用

后续讨论和重构都按这 3 条展开：

1. 继续把样片、复核、交付从主应用内部拆成清楚的模块
2. 继续把目录收成“一个代码根目录 + 子文件夹”
3. 最后再清理 `成片/` 这类历史重复目录
