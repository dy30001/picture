# @11 计划：强身份保持工具链

日期：2026-04-26  
模式：`code`  
工作区：`/Users/dy3000/code/pic`

## 1. 任务拆解

1. 检查本机环境、磁盘、Python、Docker、已有生成工具。
2. 选择最小可行身份保持路线。
3. 搭建可运行的人脸检测、相似度验收和人脸转写工具链。
4. 先修女主正面三视图，不继续扩展到 81 张。
5. 通过人工和工具链验收后，再恢复三视图批量。

## 2. 模式

`code`

原因：需要安装/配置本地工具、运行脚本、验证输出。

## 3. 目标

- 解决“脸不像”的硬阻塞。
- 建立比内置生成更强的身份保持流程。
- 输出可复现的本地工具链和验收证据。

## 3.1 用户更新

用户明确要求不要安装额外工具，继续使用现有生图工具。

本计划暂停执行，不再安装 Python 3.12、ComfyUI、InstantID、InsightFace 或其他额外组件。

后续执行回到 `planning/03-流程与提示词/02-身份一致性门禁.md` 第 8 节：仅使用生图工具，并增加逐张人工验脸门禁。

## 4. 非目标

- 当前不生成 81 张成片。
- 当前不跳过身份门禁。
- 当前不直接启用弱身份版本。
- 当前不下载完整大模型包，除非轻量方案失败且磁盘允许。

## 5. 受影响文件或区域

- `planning/03-流程与提示词/03-身份工具链计划.md`
- `tools/identity/`
- `scripts/identity_*.py`
- `review/identity_lock/`
- `generated/00_character_turnaround/`

不修改原始素材目录 `/Users/dy3000/Downloads/2002`。

## 6. 执行步骤

1. 记录环境：macOS arm64、可用空间、Python、Docker。
2. 优先尝试轻量方案：人脸检测 + embedding 相似度 + 人脸转写。
3. 如果 Python 3.14 不兼容，安装或使用 Python 3.11/3.12。
4. 建立隔离虚拟环境，不污染系统 Python。
5. 安装并验证 `insightface`、`onnxruntime`、`opencv-python` 或可替代组件。
6. 准备女主参考脸和候选图检测脚本。
7. 对当前候选 `female_front_candidate_v03.png` 做相似度基线。
8. 若可用，尝试 face swap / face restoration 修正女主正面候选。
9. 生成对比图和记录。
10. 进入 `@33` 审查。

## 7. 风险

- 当前磁盘约 25GiB，完整 ComfyUI + SDXL + ControlNet 可能空间不足。
- Python 3.14 可能与图像 AI 包不兼容。
- 轻量 face swap 可能改善脸，但无法解决侧面/背面姿态。
- 仅做人工判断不够，最好加入 embedding 分数。

## 8. 验证方案

- 虚拟环境能创建并导入关键包。
- 脚本能检测参考图和候选图中的人脸。
- 能输出相似度分数或明确失败原因。
- 生成/修正图必须与参考脸做对比图。
- `@33` 审查通过后才恢复三视图生成。

## 9. 完成标准

- 有可运行身份检测/相似度脚本。
- 有当前候选图的人脸相似度基线。
- 有明确的继续/停止判定。
- 如果修脸成功，女主正面三视图通过人工验收。
- 如果工具链失败，记录失败原因并回到用户决策。

## 10. 需要的证据

- 环境命令输出。
- 安装/导入结果。
- 检测结果。
- 相似度结果。
- 对比图。
- `@33` 审查结论。
