# 9999 端口统一现状核查 v01

Date: 2026-05-02
Mode: doc
Workspace: `/Users/dy3000/code/pic`

## 1. 一句话结论

截至 2026-05-02，当前正式运行口径已经统一到 `9999`。

更准确地说：

- 默认启动服务已经统一到 `server/index.mjs --port 9999`
- 当前健康检查入口是 `http://127.0.0.1:9999/api/health`
- 当前正式成片目录是 `final_4k/`
- 还没完全做完的是目录收口和历史文档清理，不是主入口本身

## 2. 当前已确认事实

### 2.1 启动脚本

- `package.json`
  - `dev` -> `server/index.mjs --port 9999`
  - `start` -> `server/index.mjs --port 9999`
  - `preview` -> `server/index.mjs --port 9999`

### 2.2 主服务

- `server/index.mjs`
  - 当前主服务提供主界面、模板、生成、历史、认证、积分、支付和摄影棚成片预览

### 2.3 正式成片目录

- `server/index.mjs` 当前把摄影棚成片预览目录直接指向 `final_4k/`

### 2.4 当前运行快照

- `9999` 当前正在监听
- `curl http://127.0.0.1:9999/api/health` 返回健康结果
- 当前没有必要再把 `4184` 或 `4174` 写成现行正式入口

## 3. 还没做完的部分

当前剩余问题主要是这 3 类：

1. 目录还没有完全迁成最终目标代码树
2. 旧 planning 文档里还残留历史拆分口径
3. `成片/` 这个历史重复目录还没有清理出仓

这 3 类问题都属于“收尾治理”，不是“主入口还没统一”。

## 4. 现在是不是一套系统

按运行口径回答：是。

按最终目录治理口径回答：还在继续整理。

所以当前最准确的表述是：

- 现在已经是一套主应用在 `9999` 运行
- 后面还要把目录和历史文档继续收口

## 5. 后续动作

建议后续按这个顺序继续：

1. 文档统一
   - 把所有正式规格都统一成 `9999`
   - 把 `final_4k/` 固定为唯一正式成片目录
2. 目录整理
   - 规划 `app/` 统一代码根目录
   - 把主应用模块边界进一步拆清楚
3. 历史清理
   - 清理 `成片/` 的并行正式口径
   - 把旧文档里残留的历史拆分表述收干净

## 6. 完成标准

只要下面 5 条都成立，就可以对外说“9999 已统一完成”：

1. 默认启动命令只认 `9999`
2. 当前健康检查只认 `9999`
3. 正式交付目录只认 `final_4k/`
4. 正式文档不再把历史拆分态写成现行入口
5. `成片/` 不再被脚本和正式文档当成并行正式输出
