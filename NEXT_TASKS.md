# 墨境当前总控清单

日期：2026-05-02
工作区：`/Users/dy3000/code/pic`
当前阶段：P1 样片管理产品化

## 当前结论

- 当前产品是一套统一主应用，不再按多个独立文件夹或独立系统分散展示。
- 客户前台固定 5 个一级菜单：`拍摄定制` / `图片创作` / `我的作品` / `注册开通` / `账户权益`。
- 客户页面不展示内部端口号，不放备案号，不展示连接设置和测试连接。
- 内部服务端口继续按 `9999` 统一；这是部署和运维口径，不是客户文案。
- `final_4k/` 是唯一正式成片目录；`成片/` 是历史重复目录，不再作为正式交付口径。

## P0 本轮目标

P0 只解决“对外看起来正规、内部能继续收口”的问题，不扩展 P1/P2 功能。

1. 总控清单更新
   - 本文件替代旧的婚纱批次交接口径，作为当前主应用总控入口。
2. 客户可信标识
   - 页面展示品牌、域名、流程承诺、隐私承诺和交付承诺。
   - 不展示备案号。
   - 不展示 `9999`。
3. 当前变更分组
   - 明确哪些是客户前台、哪些是服务端、哪些是文档迁移、哪些是运行数据。
4. 目录融合第一步
   - 建立 `app/` 目录壳和模块边界说明。
   - 暂不迁移运行代码，避免一次性大重构。
5. 历史文档入口清理
   - 正式入口从 `docs/README.md` 和 `planning/README.md` 进入。
   - 不再新增 `planning/` 根目录散文档。

## 当前功能目录

### 1. 拍摄定制

当前前端主线：

- `样片集`
- `生图`
- `传照片 / 三视图`
- `选模板`
- `生成`

说明：

- 这是当前更贴近客户操作的话术。
- 文档里的长期流程仍对应：先看样片，再确认身份，再选模板，再生成成片。

### 2. 图片创作

- `模板库`
- `智能生图`
- `换背景`
- `换装`
- `高清增强`
- `局部修复`

说明：

- 后四个入口当前复用同一套智能生图/编辑界面。
- 不做空菜单。

### 3. 我的作品

- `全部作品`
- `已删除`

说明：

- 管理通用生成历史和删除恢复。
- 不替代 `final_4k/` 正式成片交付目录。

### 4. 注册开通

- `注册`
- `登录`

说明：

- 客户一级菜单必须显示 `注册开通`，不再写成泛泛的 `账号`。

### 5. 账户权益

- `权益总览`
- `充值中心`
- `充值订单`
- `积分流水`

说明：

- 充值订单和积分流水分开，不再使用模糊的最近记录。

## 当前变更分组

### A. 客户前台

主要文件：

- `public/index.html`
- `public/app.js`
- `public/styles.css`
- `scripts/visual-check.mjs`
- `tests/node-server.test.mjs`

当前目标：

- 5 个一级菜单稳定。
- 客户可信标识稳定。
- 页面不出现内部端口号、备案号、连接设置和测试连接。

### B. 服务端与账户权益

主要文件：

- `server/index.mjs`
- `server/credits/`
- `server/payments/`

当前目标：

- 账户、积分、订单、支付配置继续归入同一套主应用。
- 真实支付和退款规则进入 P1，不在 P0 扩张。

### C. 文档与目录

主要文件：

- `README.md`
- `docs/README.md`
- `docs/01-文档体系规范.md`
- `planning/README.md`
- `planning/01-架构与规范/`
- `planning/02-产品与交付/`
- `planning/03-流程与提示词/`
- `planning/04-研究与交接/`

当前目标：

- 正式文档分层清楚。
- 旧根目录文档不再作为主入口。
- `app/` 目录壳先建立说明，不迁移运行代码。

### D. 运行数据与成片

主要目录：

- `generated/`
- `review/`
- `final_4k/`
- `成片/`
- `data/`

当前目标：

- `final_4k/` 是唯一正式交付目录。
- `成片/` 只保留历史兼容，不新增正式交付。
- 运行数据不混入正式代码发布口径。

## P0 完成标准

- [x] `NEXT_TASKS.md` 已更新成当前主应用总控口径。
- [x] 客户页面有可信标识，但没有备案号和内部端口号。
- [x] 一级菜单客户可见名称与当前口径一致。
- [x] `app/` 目录壳和模块边界说明已建立。
- [x] `grep` 确认客户前端不展示 `9999` 和备案号。
- [x] 通过 `npm run lint:strict`。
- [x] 通过 `npm test`。
- [x] 通过 `npm run typecheck`。
- [x] 通过 `npm run verify:visual`。

## P1 当前进展

1. 样片管理已进入前端 MVP：
   - 在 `拍摄定制 -> 样片集` 中记录单张样片状态。
   - 当前支持：`待查看 / 已入围 / 已淘汰 / 待重生 / 已转正`。
   - 当前支持动作：`入围 / 淘汰 / 需重生 / 转正记录`。
2. 样片区已增加紧凑统计摘要：
   - `待看` / `入围` / `待重生` / `已转正`。
3. 当前转正只做状态记录：
   - 正式成片口径仍只有 `final_4k/`。
   - 本轮不移动、不复制正式文件。

## P1 后续

1. 客户界面和内部状态打通。
2. `final_4k/` 与 `成片/` 历史依赖彻底清理。
3. 样片备注、修复前后对比、批量导出选片结果。
4. 积分、支付、退款、套餐价继续稳定化。
5. `换背景 / 换装 / 高清增强 / 局部修复` 增加上传规则、示例和失败提示。
6. 婚纱批次继续逐批复核。

## P2 后续

1. 品牌 SVG 母版、中文组合标、单色/反白版和印刷色值。
2. 作品管理增强：收藏、下载记录、精选成片、客户项目切换。
3. 团队/企业、AI 视频、更多隐私删除规则。

## 当前硬约束

- 不展示备案号。
- 不展示内部端口号。
- 不恢复连接设置和测试连接。
- 不新增第 6 个一级菜单。
- 不把 `成片/` 当正式目录。
- 不把 P1/P2 功能混入本轮 P0。
